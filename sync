#!/bin/sh
set -x
{
cat <<EOT
gitconfig           .gitconfig
tmux.conf           .tmux.conf
vim                 .vim
vimrc               .vimrc
go_packages         .go_packages
zshrc               .zshrc
zsh                 .zsh
EOT
} | while read origin symlink ; do
    rm -rf ~/$symlink
    ln -f -s $PWD/$origin ~/$symlink
done

